# Stellara — Environment Variables
# Copy this file to .env and fill in the values

# === Database (Neon PostgreSQL) ===
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# === OpenAI (for AI report generation) ===
# GPT-4o-mini is used for personalized analysis
OPENAI_API_KEY=sk-...

# === Stripe (for premium payments) ===
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# === Brevo (lead capture + automation) ===
BREVO_API_KEY=xkeysib_...
BREVO_LIST_ID_LEADS=9

# === Email (Hostinger SMTP) ===
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_USER=noreply@yourdomain.com
SMTP_PASS=your_password
SMTP_FROM=Stellara <noreply@yourdomain.com>

# === App ===
NUXT_PUBLIC_APP_URL=https://yourdomain.com
NODE_ENV=development
